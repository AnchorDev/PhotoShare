import { StyleSheet } from "react-native";


export const styles = StyleSheet.create({
    registerBtn: {
        padding: 25,
        backgroundColor: '#000',
        width: 300,
        marginBottom: 25
    },
    registerText: {
        color: '#fff',
        fontWeight: 'bold',
        textAlign: 'center',
        fontSize: 25
    }
})
